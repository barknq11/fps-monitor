# Normalise line endings so the repo is consistent across machines.
* text=auto eol=lf

# Windows-only launchers must keep CRLF or cmd.exe misparses them.
*.bat text eol=crlf
*.ps1 text eol=crlf

# Binaries: never touch these.
*.dll binary
*.exe binary
*.png binary
*.ico binary
*.jpg binary
